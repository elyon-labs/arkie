name: cs2_rcon_client
description: An RCON client compliant with the RCON protocol used by CS2.
version: 0.0.1
repository: https://github.com/elyon-labs/arkie

environment:
    sdk: '>=3.9.0 <4.0.0'

resolution: workspace

dependencies:
  logger: ^2.6.2
  oxidized: ^6.2.0

dev_dependencies:
  lints: ^4.0.0
  test: ^1.24.0
